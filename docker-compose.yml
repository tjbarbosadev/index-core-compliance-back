services:
  postgres:
    image: postgres:16
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: indexcore
      POSTGRES_USER: indexcore
      POSTGRES_PASSWORD: indexcore
    volumes:
      - indexcore_pg:/var/lib/postgresql/data

volumes:
  indexcore_pg:
