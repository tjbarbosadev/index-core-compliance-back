services:
  postgres:
    image: postgres:16
    ports:
      - '5433:5432'
    environment:
      POSTGRES_DB: opcore
      POSTGRES_USER: opcore
      POSTGRES_PASSWORD: opcore
    volumes:
      - opcore_pg:/var/lib/postgresql/data

volumes:
  opcore_pg:
